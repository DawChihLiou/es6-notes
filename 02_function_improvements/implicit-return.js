/*
 * Two things to note
 *  1. Implicit return is the return value follow immediately by the arrow without {}
 *  2. Arrow functions are always anonymou functions
 */
const anonymousFunction = () => 'La La Land'; // anonymousFunction returns a string
console.log(anonymousFunction());
